{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('task_edit', { 'id' : task.id }), 'attr': {'class': 'w-100'}}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success float-right">Modifier</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
