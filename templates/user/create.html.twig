{% extends 'base.html.twig' %}

{% block header_title %}<h1>Créer un utilisateur</h1>{% endblock %}
{% block header_img %}{% endblock %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('user_create'), 'attr': {'class': 'w-100'}}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success float-right">Ajouter</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
